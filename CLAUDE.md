# 1 Milhão de Influencer

Mapa permanente dos influencers do Brasil: 1.000.000 de pixels, cada bloco é um perfil.
Comunicação com o usuário e toda a copy do site: **português brasileiro**.

## Visão do produto (o que guia toda decisão)

- Plataforma para **quem está começando E quem já é grande** aparecer mais. O pequeno compra 1 pixel, o grande compra milhares — todos ganham visibilidade real (mapa, ranking, portal de marcas, link-in-bio).
- **Qualquer quantidade a partir de 1 pixel, R$ 0,99/pixel**, pagamento único e vitalício. Nunca escrever mínimo de 100px, R$ 10, R$ 0,10 ou R$ 99 como mínimo — são valores antigos.
- **Do clique até a compra no menor atrito possível.** Cada tela, campo ou passo a mais no funil precisa se justificar. Selecionou área no mapa → já cai montando o perfil.
- Depois da compra: preenchimento do perfil fácil, e benefícios claros (link-in-bio, popup com redes, propostas de marcas, ranking).

## Postura esperada do Claude

- **Seja propositivo e ousado.** Em toda conversa sobre layout/funcionalidade, sugira ativamente ideias que possam revolucionar o site — não espere ser perguntado. Ideias de crescimento viral, gamificação, prova social, urgência, novas fontes de receita.
- Ao sugerir, priorize o que reduz atrito de compra ou aumenta o motivo para voltar ao site.
- Pode implementar melhorias reversíveis de layout/copy sem pedir permissão; mudanças de escopo grandes, confirmar antes.
- **Git: commit e push para `main` automáticos, sem perguntar** (pedido do dono em jul/2026) — a Vercel builda a partir da `main`. Não se aplica a ações destrutivas (force-push, reset --hard etc.), só ao fluxo normal de commit+push.

### Decisões do dono sobre ideias (não repropor as rejeitadas)

- ✅ Aprovadas e implementadas (jul/2026): zoom no mapa com seleção até 1 pixel; pré-preenchimento de avatar/nome pelo @ (unavatar.io); arte para stories na página de sucesso; login do anunciante (`/login`); upload de foto real via Vercel Blob; selos de rede social sólidos na cor do app com pulso; estatísticas de visitas/cliques + QR code na página `/influencer/[handle]`; mídia kit auto-gerado (`/influencer/[handle]/midia-kit`) com ranking real e download em imagem; marca "Vitrine 1M" pro link-in-bio + prévia ao vivo no painel; cidade padronizada por autocomplete (IBGE); orientação (quadrado/largo/alto) e escolha manual de posição no `/comprar` sem sair da tela; foto do influencer visível direto nos blocos do mapa (canvas) e no tooltip de hover, não só no popup/página.
- ✅ Pacote de crescimento/viral (jul/2026, pedido explícito do dono "pode fazer todas as ideias"): OG image dinâmica por perfil; feed de atividade real na home; badges de marco (Fundador/Top 10 nicho/tamanho); destaque mensal no `/ranking`; banner de propostas novas no painel; sistema de indicação ligado à tabela `affiliates` (já existia no schema, sem API/UI). Detalhes técnicos de cada um em "Sistemas construídos" abaixo. Junto veio a limpeza do banco: os 14 perfis de exemplo/teste foram apagados (backup local fora do repo) — o mapa está vazio de verdade agora, sem nenhum registro fake.
- ✅ Roleta de descoberta (jul/2026, sessão "faça o que achar legal" — dono deu carta branca com o norte "ser o maior canal pra fazer as pessoas ganharem seguidores"): página `/descobrir` + pivô de copy da home para "ganhe seguidores" + botão "Seguir @handle" como CTA principal do `BlockPopup`. Detalhes em "Sistemas construídos".
- 🔁 Revisada (jul/2026): ambiente de briefing marca↔influencer — a versão original exigia a marca criar conta e logar via Clerk antes do primeiro envio ("página da marca"). Dono decidiu que **não vai ter conta/página de marca**: `BriefingModal` virou formulário único (dados da empresa + briefing juntos), sem login — `/api/proposals` cria a marca na hora por uma chave sintética a partir do e-mail. `/api/brands` foi removida. Continua podendo aceitar/recusar a proposta no painel do influencer.
- ❌ Rejeitada: preço maior em áreas "nobres" do mapa (escassez por região) — dono achou caro demais para o público.
- ❌ Rejeitada (jul/2026): vídeo em tela cheia como capa da página `/influencer/[handle]` — quem chega já veio do Instagram e já viu os Reels, seria redundante e pesaria a página.
- ❌ Rejeitada (jul/2026): mini-mapa vivo (mostrar o bloco dentro da grade real) no topo da página `/influencer/[handle]` — dono decidiu não precisar.

## Prioridade atual (julho/2026)

1. **Layout e funcionalidades** — foco agora.
2. Pagamento (bugs conhecidos) e e-mails transacionais — **depois**, quando o dono pedir.

### Pendências conhecidas — fase de pagamento/e-mails (não mexer sem o dono pedir, não esquecer)

- **MP externalId**: `payments` grava `externalId = preference.id`, mas o webhook confirma por `payment.id` → registro nunca vira `paid` (`src/lib/payments/index.ts`). Bagunça relatório de receita.
- **E-mail do comprador não é salvo**: welcome email vai para `@placeholder.com` — o comprador pode nunca receber o `editToken` (perde acesso de edição). Persistir o e-mail (em `payments` ou `blocks`) e usar o real. Ver `blockContactEmail()` em `src/lib/payments/index.ts` — é o único lugar a trocar quando isso for corrigido (também usado por `/api/auth/recover`).
- Webhook do Mercado Pago sem validação do header `x-signature`.
- Condição de corrida em `findFreePosition` — só no caminho 100% automático (ninguém escolheu posição, nem no mapa nem no `PositionPicker` do `/comprar`). Qualquer fluxo que manda `pixelX`/`pixelY` explícito (mapa OU PositionPicker) já é validado no servidor via `isAreaOccupied` (`src/app/api/payment/route.ts`).

### Pendência de segurança (rotação de credenciais — deferida pelo dono)

`.env.local` foi commitado no **repositório público** do GitHub desde o primeiro commit, expondo `DATABASE_URL`, `CLERK_SECRET_KEY` e `UPSTASH_REDIS_REST_TOKEN` reais. Parado de versionar em jul/2026 (`git rm --cached` + `.gitignore`), mas isso **não remove do histórico** — quem já clonou o repo antes continua com a cópia antiga. Dono decidiu adiar a rotação ("o site não tem nenhum acesso ainda"). Antes de qualquer lançamento real ou quando houver tráfego, lembrar de rotacionar as 3 credenciais nos respectivos dashboards (Neon/Clerk/Upstash) e atualizar `.env.local` + Environment Variables da Vercel.

### Pendências de funcionalidade (podem ser feitas a qualquer momento)

- **Upload de foto usa Vercel Blob** (trocado de Cloudflare R2 em jul/2026): `/api/upload` (`src/app/api/upload/route.ts` + `src/lib/storage/index.ts`, pacote `@vercel/blob`). Store conectada e testada ponta a ponta em jul/2026 — **a store precisa ser criada como "Public" desde o início**; o modo de acesso não pode ser trocado depois (só apagar e recriar). `ImageUpload.tsx` expõe `onUploadingChange` — painel e cadastro travam o botão principal enquanto o upload está em voo, porque já rolou de alguém salvar o resto do perfil com a foto ainda subindo e o `avatarUrl` ir vazio pro banco (corrigido jul/2026).
- Zero testes no projeto.
- Zoom do mapa no mobile: os botões funcionam, mas o pan (arrastar o mapa com zoom) conflita com a seleção por toque — melhorar gesto (ex.: dois dedos para pan/pinch).
- `LiveVisitors` (contador de "pessoas no mapa agora") continua simulado de propósito — decisão de produto revisitada em jul/2026: converter pra presença real hoje, com tráfego ~zero, mostraria um número baixo/vazio e prejudicaria mais que ajudaria. Trocar quando houver volume real. **Não confundir** com o `ActivityFeed` ao lado dele (feed de atividade — "@x garantiu N pixels"), que já é 100% real e some sozinho quando não há nada recente; nem com as estatísticas do painel/mídia kit (visitas, cliques, ranking), que também são reais, vêm da tabela `analytics` e de contagem direta em `blocks`.
- Sistema de indicação (`affiliates`) tem link/cliques/cadastros funcionando, mas **não calcula recompensa em dinheiro automaticamente** — `totalEarned`/`totalPaid` ficam em 0, só a chave PIX é coletada. **Decisão explícita do dono (jul/2026): formato de remuneração fica pendente de propósito — ele mesmo decide isso quando for mexer no checkout/pagamento** (fixo por indicação? percentual? pixels em vez de dinheiro? ver as opções que já foram levantadas). Quando o formato for decidido, ligar o cálculo em `POST /api/payment` (mesmo lugar que já incrementa `totalSignups` — ver `src/lib/db/affiliates.ts`).
- Hydration mismatch conhecido em `PainelClient.tsx` (aba Perfil): `profileUrl` usa `window.location.origin` no client mas renderiza vazio/relativo no server — gera warning no console, não quebra a função. Não corrigido ainda.
- `BlockPopup.tsx` e `PixelGrid.tsx` mantêm suas próprias cópias de config de redes sociais (cores/ícones), diferentes de `src/lib/socialConfig.tsx` (usado por `/influencer/[handle]` e pelo mídia kit) — se for mexer visualmente nos selos de rede social, checar os três lugares.

## Stack e comandos

Next.js 15 (App Router) + TypeScript + Tailwind · Neon/Drizzle · Upstash Redis · Clerk · Mercado Pago + Stripe · Ably · Resend · Vercel Blob (storage) · Vercel. Estrutura detalhada no `README.md`.

**Produção**: https://1milhao-sigma.vercel.app (projeto Vercel `felipeff-s-projects/1milhao`, deploy automático a partir da `main`).

⚠️ **jul/2026**: todas as Environment Variables de Production/Preview estavam cadastradas só de nome, com valor **vazio**, desde a criação do projeto (44 dias) — o site em produção não tinha banco, Clerk, Redis nem `NEXT_PUBLIC_APP_URL` funcionando. Corrigido: DATABASE_URL, Clerk, Upstash e `NEXT_PUBLIC_APP_URL` (+ variáveis inertes tipo `NEXT_PUBLIC_PIXEL_PRICE`) agora têm valor real; R2 antigo foi removido. Stripe/Mercado Pago/Ably/Resend continuam vazios em produção (sem credenciais reais ainda — combinado com pendências abaixo).
Se mexer em env vars pela Vercel CLI: **`vercel env pull` grava os valores entre aspas literais no `.env.local`** — um script de shell (`grep`/`cut`) que reler esse arquivo pega a aspa junto e quebra o valor ao reenviar (`sed -e 's/^"//' -e 's/"$//'` resolve). E **`vercel redeploy` reaproveita o snapshot de env vars do deploy antigo** — pra aplicar uma env var nova em produção, precisa de `vercel deploy --prod` (ou um commit novo), não `redeploy`.

```bash
npm run dev          # dev server (Turbopack)
npm run typecheck    # tsc --noEmit — rodar antes de encerrar mudanças
npm run build
npm run db:push      # schema → banco
```

`npm run db:push` (drizzle-kit) pode abrir um prompt interativo de seleção (setas + Enter) quando não consegue decidir sozinho se uma coluna foi "criada" ou "renomeada" — isso não funciona bem via terminal não-interativo (piped stdin não é lido). Se travar nisso, aplicar a alteração via SQL direto (script `tsx` com `neon()` + `ALTER TABLE`) e depois rodar `drizzle-kit push --force` de novo só para confirmar que ficou igual ao schema (aí ele não pergunta nada, só aplica o resto).

**Testar visualmente sem depender do usuário**: não há Playwright no projeto (não é dependência fixa), mas dá pra instalar temporariamente com `npm install --no-save playwright` (não mexe no `package.json`) + `npx playwright install chromium`, escrever um script `.js` temporário na raiz do projeto (fora da raiz o Node não acha `node_modules`) chamando `chromium.launch()`, e apagar tudo depois. Scripts de banco avulsos (`tsx algumascoisa.ts`) também precisam ficar na raiz do projeto pelo mesmo motivo — e não podem importar arquivos que tenham `import 'server-only'` no topo (ex. `src/lib/db/blocks.ts`, `src/lib/realtime`, `src/lib/email`), pois esse pacote lança erro fora do bundler do Next; nesses casos, replicar a query/lógica direto no script com Drizzle puro.

Se criar/apagar blocos direto no banco pra teste (bypassando as rotas normais), **lembrar de invalidar o cache Redis** (`blocks:all`, `grid:stats`) — senão o mapa mostra dado desatualizado por até 5 min, e como o Redis é o **mesmo do banco de produção** (não é um Redis local), isso pode vazar pra quem tiver acessando o site de verdade nesse intervalo. Um script `tsx` isolado às vezes falha com `ENOTFOUND` no hostname do Upstash (DNS flakiness específico de processo `tsx` avulso nesse ambiente, não acontece dentro do próprio dev server) — se isso travar, criar uma rota temporária em `src/app/api/<algumnome>/route.ts` (sem `_` no início — pastas com `_` são ignoradas pelo router do Next) chamando `invalidateBlocksCache()` e bater nela via `curl` com o dev server rodando, depois apagar a rota. Sempre limpar dados de teste do banco (e do cache) antes de encerrar.

## Sistemas construídos em jul/2026 (mapa de orientação)

- **Login do anunciante** (`/login`): cola link/token de acesso, ou "esqueci meu link" por @ (reenvia e-mail — sujeito à mesma pendência de e-mail do comprador acima).
- **Upload de foto** (`/api/upload` → `src/lib/storage/index.ts`): Vercel Blob, usado no cadastro e no painel (`ImageUpload.tsx` compartilhado).
- **Analytics real do perfil**: `ProfileTracking.tsx` dispara `view` no load da página e delega cliques via atributo `data-track` (`social:<campo>`, `custom_link:<índice>`, `advertise_click`, `share`, `qr_generate`) para um único listener — sem precisar transformar cada link em client component. `/api/track` aceita qualquer string alfanumérica como `eventType` (antes era um enum fixo que descartava silenciosamente vários cliques do `BlockPopup`). Agregação em `src/lib/db/analytics.ts` (`getBlockStats`, últimos 7 dias), exibida na aba "📊 Stats" do painel.
- **Ranking real**: `getBlockRanking(blockId)` em `src/lib/db/blocks.ts` — posição geral e por nicho, calculada por contagem (não é simulado).
- **Ambiente de briefing marca↔influencer**: tabelas `brands` e `proposals` (já existiam no schema, nunca tinham API/UI — só liguei). Fluxo atual (revisado, sem Clerk — ver "Revisada" acima): `BriefingModal`/`SendBriefingButton` (`src/components/proposals/`) → `POST /api/proposals` (recebe dados da empresa + briefing juntos, cria/reaproveita a `brand` por `getOrCreateBrandByEmail` — chave sintética `email:<endereço>` na coluna `clerkUserId`, sem migração de schema) → aba "💼" no painel (`getProposalsForBlock`, `markProposalsRead`) → `POST /api/proposals/[id]/respond` (aceitar/recusar, valida por `editToken`). Tabela `favorites` (marca salva influencer) continua existindo no schema mas segue sem uso.
- **Mídia kit** (`/influencer/[handle]/midia-kit`): página com dados reais (segue `src/lib/socialConfig.tsx`, compartilhado com o perfil) + botão de baixar como PNG (`DownloadKitButton.tsx`, gera num canvas maior e recorta pela altura real do conteúdo — evita sobra de espaço em branco).
- **Vitrine 1M** (nome de marca pro link-in-bio, cunhado jul/2026): no painel, card explica que o formulário alimenta essa mini página pública + botão "Ver prévia ao vivo" abre `VitrinePreviewModal.tsx` (mock fiel da página pública com os dados atuais do form, antes de salvar).
- **Cidade padronizada**: `CityAutocomplete.tsx` + `src/lib/cities.ts` (busca municípios do IBGE uma vez, cacheia em memória, filtra local por prefixo/substring) — usado no cadastro e no painel. Antes era texto livre sem padrão nenhum.
- **Foto do influencer no mapa**: corrigido bug onde `PixelGrid.tsx` checava um campo (`_img`) que nunca era preenchido — a foto nunca aparecia no canvas do mapa, só no popup/tooltip/página. Agora tem cache de `Image()` por bloco (chave `id+avatarUrl`, recarrega se a foto mudar) e a foto cobre o bloco inteiro (era 35% opacity, quase invisível). Tooltip de hover também ganhou faixa de foto cheia no topo (sem borda entre foto e texto, funde com o conteúdo) no mesmo estilo do popup de clique.
- **`/comprar` sem vir do mapa**: Step 1 ganhou orientação (`⬛ Quadrado`/`▬ Largo`/`▮ Alto`, mantém ~mesma área) e `PositionPicker.tsx` — mini-mapa interativo (busca ocupação via `/api/blocks`) pra clicar numa posição livre ou pedir aleatória, sem sair da tela. Opcional: sem escolher nada, cai no automático de sempre (`findFreePosition`).
- **Gotcha corrigido**: `getActiveBlocksForGrid()` (`src/lib/db/blocks.ts`) selecionava só um subconjunto de colunas — faltavam `whatsappUrl` e as outras redes sociais, então o popup do mapa nunca sabia que o influencer tinha WhatsApp/YouTube/etc (caía sempre no fallback de Instagram). Se adicionar um campo novo em `blocks` que precisa aparecer no mapa/popup/tooltip, lembrar de incluir nesse select também — é fácil esquecer porque a página `/influencer/[handle]` usa outra query (`getBlockByHandle`, seleciona tudo) e não tem esse problema.
- **OG image dinâmica** (`src/app/influencer/[handle]/opengraph-image.tsx`, convenção de arquivo do Next — `next/og`/`ImageResponse`): gera o card de compartilhamento (foto, nome, @, nicho/cidade, seguidores/pixels) na hora, por handle. `generateMetadata` em `page.tsx` não declara mais `openGraph.images` manualmente — deixa o arquivo cuidar sozinho. **Cuidado com emoji/glyphs incomuns no satori** (motor que renderiza) — um "✦" quebrou (caixa vazia), trocado por texto puro; testar visualmente qualquer símbolo novo antes de confiar.
- **`ActivityFeed`** (`src/components/ui/ActivityFeed.tsx`, em `HeroSection.tsx` ao lado do `LiveVisitors`): cicla pelas últimas ativações reais (`getRecentActivations` em `blocks.ts`). Renderiza `null` (nada) se não houver blocos ativos recentes — não inventa número quando está vazio.
- **Badges de marco** (`getBlockBadges(blockId)` em `blocks.ts`, tipo `Badge` já existia em `src/types` sem uso): Fundador (100 primeiros por ordem de cadastro, não por tamanho), Top 10 do nicho (só se o nicho já tiver 3+ concorrentes, senão vira brinde vazio), faixa de tamanho (100/900/2.500/10.000px). Exibido via `BadgeRow.tsx` no perfil público e no painel.
- **Destaque mensal no `/ranking`** (`getTopThisMonth()` em `blocks.ts` + `TopThisMonth.tsx`): quem mais cresceu no calendário atual, filtro só por `createdAt`. Aditivo — o ranking principal continua por `pixelCount` total pra sempre, não reseta (coerente com "vitalício").
- **Sistema de indicação** (tabela `affiliates` já existia no schema — `refCode`, `totalClicks`, `totalSignups`, `totalEarned`, `totalPaid`, `pixKey` — nunca tinha API/UI, só liguei): `src/lib/db/affiliates.ts` (`getOrCreateAffiliate`, `refCode = instagramHandle`, já único). `POST /api/ref/click` conta clique em `/comprar?ref=<handle>` (best-effort, nunca trava o fluxo de compra). `POST /api/payment` conta `totalSignups` quando `ref` vem no corpo e não é auto-indicação. Aba "🎁" no painel mostra link, cliques, cadastros e campo de chave PIX (`GET`/`POST /api/affiliate`, autenticado por `editToken` como o resto do painel). Ver pendência acima sobre `totalEarned` não ser automático.

- **Pacote "site vivo" (jul/2026, feedback do dono: "não tem nenhum efeito bonito, parece estático")**: animações globais em `globals.css` (`gold-sheen` varrendo todo `.btn-gold`, `.reveal`/`.reveal-visible`, `pixel-drift`, `animate-title-glow`) + `src/components/ui/Reveal.tsx` (`<Reveal>` scroll-reveal com IntersectionObserver e delay pra stagger; `<CountUp>` contador animado). Aplicado em todas as seções da home e no hero (partículas de pixel flutuando, stats com CountUp). Tudo com fallback `prefers-reduced-motion`. Junto veio a **linguagem oficial do produto** (pedido do dono): vender pros dois públicos ao mesmo tempo — "do primeiro seguidor ao próximo milhão" / quem tá começando ganha os primeiros seguidores, quem já é grande multiplica. Está no hero, nas tags de público dos planos de preço, em 2 FAQs e no FooterCTA. Manter esse tom em copy nova.
- **Força do perfil** (`src/components/forms/ProfileStrength.tsx`, jul/2026): medidor ao vivo de completude do perfil (pontos por foto/bio/rede extra/WhatsApp/cidade/seguidores/link custom, níveis Começando→Imbatível) com top-3 dicas do que falta. Usado no painel (acima das abas, reage a qualquer aba) e no cadastro (Step 2, embaixo do preview). Recebe o objeto `form` direto + `customLinksCount`. No mesmo commit: `RegisterForm` ficou responsivo (grids `grid-cols-5` viraram `grid-cols-1 sm:grid-cols-5` — antes o mobile espremia preview e campos lado a lado).
- **Página de venda do link na bio** (`/vitrine`, jul/2026, pedido do dono): vende a Vitrine 1M como produto tipo Linktree — R$ 0,99 único e **vitalício** (link + pixel no mapa inclusos), bloco "sem surpresas" explícito, grid de funcionalidades, comparativo assinatura vs vitalício, CTA `/comprar?pixels=1` (upsell de bloco maior fica pro fluxo normal do /comprar). Usa `src/components/ui/social-links.tsx` (componente adaptado de shadcn/framer-motion, pedido com referência visual): nomes das redes em linha e a logo salta acima da palavra no hover, tiles com cor/ícone oficiais vindos do `SOCIAL_CONFIG` compartilhado; sem mouse (celular/idle) cicla sozinha por tempo (`autoCycleMs`, 0 desliga). Trouxe `framer-motion` como dependência nova e criou `src/lib/utils.ts` (`cn` = clsx + tailwind-merge). Linkada no menu mobile e no footer. **Revisão do dono (jul/2026)**: hover do SocialLinks cobre a área da logo inteira (não só a palavra); CTA virou "Pegar meu link na bio vitalício" com explicação de que o link vem junto do espaço no mapa; "TUDO ISSO **POR** R$ 0,99" (ele estranhou "NOS"); cards de benefício ganharam efeito de holofote seguindo o mouse (`VitrineFx.tsx` — `SpotlightCard`, borda que acende na cor de cada card); comparativo virou "ENQUANTO VOCÊ LÊ, A ASSINATURA COBRA" com relógio ao vivo (+R$ 29 por tick de 1,3s no lado assinatura, Vitrine congelada em R$ 0,99, medalha VS central). `src/components/ui/shape-landing-hero.tsx` (kokonut/shadcn adaptado, dep `lucide-react`): `ElegantShape`/`BrandShapes` = formas de vidro flutuantes nas cores da marca, usadas no fundo do hero da home e da /vitrine; hero da home também ganhou a fileira SocialLinks abaixo dos CTAs linkando /vitrine. **Exemplo vivo (jul/2026, pedido do dono)**: botão "👀 Ver um exemplo pronto" na /vitrine (topo e CTA final) abre `ExemploModal` (em `VitrineFx.tsx`) — phone-frame com uma Vitrine modelo preenchida usando o Instagram real @rafaellassantosoficial (foto via unavatar, fallback iniciais). Todos os links do exemplo apontam pro Instagram real (os outros perfis não são oficiais); contatos são fictícios e sinalizados — **decisão consciente: o WhatsApp falso é só exibido, sem link wa.me**, pra não mandar visitante pro zap de um estranho. Overlay fecha com Esc/clique-fora e tem CTA de compra dentro ("Quero a minha assim").
- **Roleta de descoberta** (`/descobrir`, jul/2026): `page.tsx` (server, usa `getActiveBlocksForGrid` + `getGridStats`) + `DescobrirClient.tsx`. Sorteia perfis reais um a um com animação de giro (flashes rápidos + settle), filtro por nicho (chips só de nichos presentes, com contagem), giro por tecla espaço, e botão "Seguir @handle" com gradiente do Instagram. Estado vazio honesto (sem perfil fake) com CTA de compra. Analytics: settle do giro dispara `view` (1x por bloco por sessão, `viewedRef`), follow dispara `social:instagramUrl` — tudo cai nas stats reais do painel. Com o mapa vazio entra o **modo demo** (`DemoRoleta`, no mesmo arquivo): gira "vagas abertas" por nicho sozinha a cada 5s (sem perfil fake, dispara zero analytics), CTA "Garantir esta vaga" — a página nunca aparece parada. Usa o `SOCIAL_CONFIG` compartilhado de `src/lib/socialConfig.tsx`. Linkada no hero da home (botão 🎰), no menu mobile e no footer. Junto veio: `BlockPopup` ganhou "Seguir @handle" (gradiente IG) como CTA principal — Anunciar/Ver página viraram secundários — e o tracking do popup foi corrigido de `social_<key>` para `social:<key>` (antes os cliques de rede do popup nunca apareciam nas stats do painel, `getBlockStats` só reconhece o prefixo `social:`); `searchBlocks` trocou `like` por `ilike` (cidade e keyword); "Sou uma marca" saiu dos botões do hero e virou link discreto abaixo dos CTAs.

## Convenções do código

- Copy sempre pt-BR, tom direto e vendedor; visual dark com dourado (`gold`), rosa (`pink`) e cores de nicho (`NICHE_COLORS` em `src/types`).
- **Contraste**: texto sobre fundo escuro nunca abaixo de `text-white/40` (e /40 só para o mínimo decorativo, ex. copyright). Escala em uso: /55 rótulos discretos · /60–/65 texto de apoio · /70+ corpo. Não reintroduzir /15–/35.
- Arquivos de componente/página SEMPRE com extensão `.tsx` — já houve rota quebrada por `page` sem extensão em `src/app/comprar/sucesso/`.
- Preço/regra de negócio: `PIXEL_PRICE = 0.99` aparece duplicado em `src/lib/payments/index.ts`, `HeroSection.tsx` e `RegisterForm.tsx` — se mudar, mudar em todos (ou centralizar).
- Seções da home ficam em `src/components/grid/sections.tsx` (HowItWorks, Pricing, Ranking, SocialProof, FAQ, FooterCTA); `HowItWorksSection.tsx`/`PricingSection.tsx` são só re-exports.
- Seleção livre no mapa grande vem por query params `?pixels&x&y&w&h` para `/comprar` (`step` pula direto pro 2). Sem esses params, o Step 1 calcula `width×height` por `calcDims(pixels, orientation)` — quadrado por padrão, mas `orientation` (`square`/`wide`/`tall`, aspecto ~2:1) pode mudar isso — não é mais sempre `side²`. Posição manual (`PositionPicker`) é opcional e independente da posição vinda do mapa.
